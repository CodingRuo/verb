{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "bun --watch run src/index.ts",
    "dev:server": "concurrently \"bun dev:db\" \"nuxt dev\" \"bunx drizzle-kit studio\" ",
    "dev:db": "turso dev --db-file local.db",
    "dev:studio": "bunx drizzle-kit studio",
    "email": "email dev --dir src/emails"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.3",
    "elysia": "^1.3.5",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
